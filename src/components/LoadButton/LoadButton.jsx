import React from "react";
import classes from "./loadbutton.module.css";

const LoadButton = () => {
  return <button className={classes.loadButton}>Load more stories</button>;
};

export default LoadButton;
